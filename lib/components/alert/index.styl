@import '../../stylesheets/defaults'

+prefix-classes('mp-alert-')
  .container
    display flex
    flex-direction column

      .body
        display inline-flex
        text-align left
        min-height 92px
        padding 15px 30px 0 20px
