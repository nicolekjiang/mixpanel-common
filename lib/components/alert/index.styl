@import '../../stylesheets/defaults'

+host-styles(mp-alert)
  width 300px

+prefix-classes('mp-alert-')
  mp-modal
    width inherit

  .container
    display flex
    flex-direction column
    margin-right 40px

    .body
      display inline-flex
      margin 12px 10px 10px

    .icon
      display inline-block
      svg-icon-style(red-300)
      vertical-align middle

    .copy
      display inline-flex
      flex-wrap wrap
      margin-left 10px
